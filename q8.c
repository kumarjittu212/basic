//write a program in c to display the size of every data type using "size of" operator
#include<stdio.h>
#include<conio.h>
void main()
{
     printf("\n display the size of the integer data type is %d",sizeof(int));
     printf("\n display the size of the character data type is %d",sizeof(char));
     printf("\n display the size of the float data type is %d",sizeof(float));
     printf("\n display the size of the double data type is %d",sizeof(double));
}