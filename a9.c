// write to illustrate the use of unary prefix ans postfix imcrement and decrememt operators.
#include <stdio.h>
#include <conio.h>
void main()
{
    int x = 10, y = 15;
    printf("x=%d y=%d", x, y);
    x++;
    printf("\nincremet of value of x=%d", x);
    y--;
    printf("\n decrement of value of y=%d", y);
    getch();
}